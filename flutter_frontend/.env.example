# Flutter frontend environment.
# Copy to .env and adjust as needed. The app reads this at runtime.
# BACKEND_BASE_URL must point to the Flask backend base URL (port 3001).
# REALTIME_PROTOCOL should be 'sse' to enable realtime updates.
#
# Examples:
# For local emulator:
# BACKEND_BASE_URL=http://10.0.2.2:3001
# For localhost on device (rarely works unless tunneled):
# BACKEND_BASE_URL=http://localhost:3001
# For preview host:
# BACKEND_BASE_URL=https://vscode-internal-10014-beta.beta01.cloud.kavia.ai:3001
# REALTIME_PROTOCOL=sse

BACKEND_BASE_URL=http://10.0.2.2:3001
REALTIME_PROTOCOL=sse
