# Flutter frontend environment example
# Copy to .env and adjust as needed for your environment.

# Point to the Flask backend base URL (port 3001)
BACKEND_BASE_URL=http://localhost:3001

# Realtime protocol: set to 'sse' to enable server-sent events, anything else to disable
REALTIME_PROTOCOL=sse
